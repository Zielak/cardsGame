{
  "version": "0.10.0",
  "packages": ["packages/*"],
  "command": {
    "publish": {
      "ignoreChanges": ["*.md", "test/**"],
      "message": "chore(release): publish %s"
    },
    "version": {
      "allowBranch": "master"
    }
  }
}
