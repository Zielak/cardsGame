{
  "version": "0.12.3",
  "packages": ["packages/*"],
  "command": {
    "publish": {
      "ignoreChanges": ["*.md", "test/**"],
      "message": "chore(release): publish %s"
    },
    "version": {
      "allowBranch": "master"
    }
  }
}
