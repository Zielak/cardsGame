{
  "$schema": "https://v2-9-3.turborepo.dev/schema.json",
  "tasks": {
    "build": {
      "inputs": ["src/**", "tsconfig.json"],
      "dependsOn": ["^build"],
      "outputs": ["lib/**", "dist/**"]
    }
  }
}
