// config for both working with IDE and test runs
{
  "extends": "@cardsgame/base-configs/tsconfig.json",
  "compilerOptions": {
    "noEmit": true,
    "lib": ["ES2020", "DOM"],
    "types": ["@cardsgame/types", "jest", "node"]
  }
}
