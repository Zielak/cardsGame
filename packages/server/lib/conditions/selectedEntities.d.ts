import { ServerPlayerEvent } from "../player";
export declare const matchRank: (_ranks: string | string[]) => (_: any, event: ServerPlayerEvent) => boolean;
declare const _default: {
    matchRank: (_ranks: string | string[]) => (_: any, event: ServerPlayerEvent) => boolean;
};
export default _default;
