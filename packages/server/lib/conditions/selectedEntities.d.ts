import { PlayerEvent } from "player";
export declare const matchRank: (_ranks: string | string[]) => (_: any, event: PlayerEvent) => boolean;
declare const _default: {
    matchRank: (_ranks: string | string[]) => (_: any, event: PlayerEvent) => boolean;
};
export default _default;
