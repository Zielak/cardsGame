{
  "name": "cardsgame",
  "description": "not for publishing",
  "main": "index.js",
  "author": "",
  "license": "ISC",
  "scripts": {
    "build": "",
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write \"**/*.{js,json,html,scss,ts,tsx,md}\"",
    "preversion": "npm ci && npm run test"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "lint-staged": {
    "*.{ts,tsx,json,md}": [
      "prettier --write",
      "git add"
    ]
  },
  "devDependencies": {
    "@types/bezier-js": "0.0.8",
    "@types/jest": "^24.0.9",
    "@types/pixi.js": "^4.8.6",
    "@types/react": "^16.8.5",
    "@types/react-dom": "^16.8.2",
    "husky": "^1.3.1",
    "jest": "^24.1.0",
    "lint-staged": "^8.1.5",
    "parcel-bundler": "^1.11.0",
    "prettier": "^1.16.4"
  }
}
